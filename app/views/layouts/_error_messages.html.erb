<% if model.errors.any? %>
  <div class="alert">
    <h4><%= pluralize(model.errors.count, "error") %> prohibited this product from being saved: </h4>
    <ul>
      <% model.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>